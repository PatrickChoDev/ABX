# Adorable kernel But not UNIX

This kernel is made from `zero line of code` with [Rust](http://rust-lang.org). I'm not sure wheter it faster than GNU/[Linux](http://kernel.org). This kernel is for **my hobbies** and **educational purpose** only